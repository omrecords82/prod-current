/**
 * useProfileSync Re-export
 * 
 * Re-exports useProfileSync from the hooks directory for convenience.
 * This allows importing from lib/useProfileSync instead of the full hooks path.
 */

export { useProfileSync } from '../hooks/useProfileSync';
