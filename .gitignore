# --- Screenshots & Playwright artifacts ---
*.png
*.jpeg
*.jpg
.playwright-mcp/

node_modules/
**/node_modules/
dist/
**/dist/
build/
**/build/
misc/
/ops/
/logs/
server/logs/
bigbook/logs/
*.log
npm-debug.log*
yarn-debug.log*
.env
.env.*
*.env
# Allow .env.example templates to be tracked (no secrets, just documented var names).
!**/.env.example
# --- Secrets & certificates (specific file types, not broad name matches) ---
**/*.pem
**/*.crt
**/*.cert
**/*.p12
**/*.pfx
**/credentials.json
**/service-account*.json
**/*-credentials.json
**/dumps/**

# --- Local OS / IDE ---
.DS_Store
.idea/
.vscode/
*.iml

# --- Local caches / agent artifacts ---
.cache/
_cursor_session_backup/
.trash_dedupe/
.trash_dedupe_conflicts/

# --- Logs / archives / large artifacts ---
*.zip

# --- Runtime uploads / generated OCR artifacts (must not be in git) ---
uploads/
server/uploads/
server/scripts/backups/

# --- OS / Editor ---
.DS_Store
*.swp
*.swo
*.bak
*.old
*~
*.tmp

# --- IDE / tooling ---
.idea/
.vscode/

# --- Local caches / agent artifacts ---
.cache/
_cursor_session_backup/
.trash_dedupe/
.trash_dedupe_conflicts/

# --- Python virtualenvs (should never be inside front-end) ---
.venv/
venv/
**/.venv*
front-end/.venv_image/

# --- Logs / archives ---
*.zip

# --- Runtime / uploaded content (never version control) ---
uploads/
server/uploads/

# --- Local analysis artifacts ---
git-status.txt
identify_conflicts.md
om_structure_tree.txt
*.prod-backup
.analysis/

# Docs are tracked under repo-root /docs (canonical).
# Do NOT ignore /docs; enforce doc placement via CI checks instead.

# --- Local backups / scratch ---
.gitignore.bak.*
.github/.commit-templates/
ORTHODOXMETRICS_CHANGE_LOG.md

# --- Non-canonical docs folders (keep only README pointers) ---
front-end/docs/**
!front-end/docs/README.md
front-end/public/docs/**
!front-end/public/docs/README.md
server/docs/**
!server/docs/README.md

# --- Build & Distribution Artifacts ---
.build-info
front-end/public/assets/
front-end/public/build-info.json
front-end/public/index.html
front-end/dist-latest/
front-end/dist-stable/
front-end/dist-backup/
front-end/dupes/

# --- Test / scratch data ---
test-data/
screenshots/
server/data/

# --- Server Backups & Temp Storage ---
server/storage/
server/temp-backups/
server/webhooks/
server/integrations/
legacy_audit/
*.backup
*.prod-backup
test-config.js

# --- Project Specific Artifacts ---
front-end/notification.txt
omai-ecosystem.config.js
orthodoxmetrics-external.com
orthodoxmetrics-internal.com

# --- Documentation & Tools ---
tools/audit.js
# Local backups and scratch
backups/
copies/

# --- Agent / IDE workspace artifacts ---
c0/
c1/
c2/
c3/
cursor/
ws/
.windsurf/
.mcp.json
WS-tasks.md
conversation-tasks.json
config/agenttasks-*.json
followup.txt
reports/
prod-backup/
front-end/server/
# server/scripts/ is tracked — contains build tooling required for fresh clones.
# Local backups under server/scripts/backups/ are still ignored (see earlier rule).
.snapshots/
.branch-notes.json

# Added via Repo Operations
orthodoxmetrics.com
inner-orthodoxmetrics.com

# Root-level scripts/ currently lives outside the repo (in /var/omai-ops/scripts/orthodoxmetrics/).
# Anchored with a leading slash so this only ignores the root dir and does NOT
# match server/scripts/ (which is tracked for build tooling).
/scripts/

# Docs live in /var/omai-ops/docs/orthodoxmetrics/ (outside repo)
docs/

# Berry frontend lives in /var/www/omai/berry/ (canonical copy)
berry/

# Analysis artifacts
.analysis/

# Deploy artifact (auto-generated)
.last-deploy.json
front-end/node_modules
